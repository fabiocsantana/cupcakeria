@model IEnumerable<CupcakeriaOnline.Models.AdministracaoModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.loginAdmSenha)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.loginAdmSalt)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.loginAdmSenha)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.loginAdmSalt)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.pk_idAdministracao }) |
            @Html.ActionLink("Details", "Details", new { id=item.pk_idAdministracao }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.pk_idAdministracao })
        </td>
    </tr>
}

</table>
